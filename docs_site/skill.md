# -[ Aima UTG Agent-Skill Discovery ]- 🦞
# Version: 1.2.0
# Last Updated: 2026-04-21

## 🤖 Introduction for Agents
You are an AI Agent (e.g., Moltbook, OpenClaw, or Claude). You have been granted access to the **Aima Universal Transaction Gateway (UTG)**. This protocol allows you to execute high-stakes financial settlements securely.

> [!IMPORTANT]
> **GOVERNANCE RULE 0:** You are a non-custodial actor. You DO NOT have access to private keys. You MUST request a "Signature Share" (PIN) from the human user for any transaction exceeding $0.00.

---

## 🛠️ MCP Connection String
To use these skills, add the following to your `mcp_servers` configuration:

```json
"aima_utg": {
  "command": "python",
  "args": ["/absolute/path/to/universal-transaction-gateway/src/gateway/server.py"],
  "env": {
    "ETHEREUM_RPC_URL": "...",
    "AIMA_API_KEY": "..."
  }
}
```

---

## 🏗️ Protocol Stack (The 3 Golden Rules)

| Rule | Rule Name | Agent Requirement |
| :--- | :--- | :--- |
| **01** | **Safety Sandwich** | Any `eth_transfer` call will HALT. You must catch the halt, ask the User for their PIN, and call `submit_signature_share` with the PIN to resume. |
| **02** | **Strict Idempotency** | You may retry tools if you hit network timeouts. The Gateway will mathematically prevent double-spending using your `id_key`. |
| **03** | **Ephemeral Vault** | Once a job is SUCCESSFUL, the Gateway will WIPE all your signatures. Do not attempt to re-use transaction IDs. |

---

## 📦 Available Tools

### 1. `defi_eth.request_eth_transfer_reliable`
- **Purpose**: Moves ETH between addresses.
- **Safety**: Triggers a mandatory 6-digit PIN requirement.
- **Strategy**: Call this tool, receive the `txn_id`, then message the human: *"I need your 6-digit Gateway Passcode to finalize this $AMOUNT ETH transfer."*

### 2. `handover.submit_signature_share`
- **Purpose**: Finalizes a halted transaction.
- **Usage**: Use this ONLY after a human has provided their PIN in the chat channel.

---

## 💓 Heartbeat & Verification
To verify the gateway is online, call `get_a2a_agent_card`. If it returns a JSON object with `capabilities: ["legal_vault"]`, the protocol is healthy.

---
*Generated by Aima Engineering. Approved for Agentic Discovery.*
